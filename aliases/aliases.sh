# Helmut Tool-Box aliases BEGIN

alias pull='echo "git pull" && git pull'
alias push='echo "git push" git push'
alias pullstash='git stash && git pull && git stash apply --index'
alias codebashrc='echo "code ~/.bashrc" && code ~/.bashrc'
alias loadbashrc='source ~/.bashrc'
alias status='git status'
alias commit='git add . && git commit -m'
alias idea='/opt/idea-*/bin/./idea'
alias resetidea='rm -r ~/.java/.userPrefs/'
alias executable='chmod +x'

# Helmut Tool-Box aliases END"
